interface:
  display_name: "App Version Bump"
  short_description: "Refresh manifest version after changes"
  default_prompt: "Use $app-version-bump to update manifest.json version after app changes so the service worker refreshes cached data and assets."
